<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hedtrot.perspective" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Main"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="com.google.intent.category.CARDBOARD" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Autorama" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Will open URIs that begin with "hedtrot://autorama" --><!-- em tese-->
                <data android:scheme="hedtrot"
                    android:host="autorama" />
            </intent-filter>
        </activity>
        <activity android:name=".Card" >
        </activity>
        <activity android:name=".Car" >
        </activity>
        <activity android:name=".CardViews" >
        </activity>
        <activity android:name=".Lab" >
        </activity>
    </application>

</manifest>
